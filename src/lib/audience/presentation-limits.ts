/** How many audience segments to show in breakdown tables and charts. */
export const TOP_SEGMENT_DISPLAY_COUNT = 8;

/** How many ZIP codes to show in ranked lists (with USPS city labels). */
export const TOP_ZIP_DISPLAY_COUNT = 8;
